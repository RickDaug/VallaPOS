{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the VallaPOS desktop window: core APIs plus the SQLite and store plugins.",
  "windows": ["main"],
  "permissions": ["core:default", "sql:default", "store:default"]
}
